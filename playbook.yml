---
- name: Install and start Apache(httpd)
  hosts: webserver
  become: yes
  gather_facts: yes
  tasks:
    - name: Install Apache2
      apt:
        name: apache2
        state: present
        update_cache: yes

    - name: start apache2
      service:
        name: apache2
        state: started
        enabled: yes

